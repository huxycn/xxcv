from setuptools import setup, find_packages

setup(
    name='yolo',
    version='0.0.1',
    author='huxycn',
    author_email='huxycn@foxmail.com',

    install_requires=[
    ],

    packages=find_packages()
)